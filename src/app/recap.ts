const username: string = 'deivith';
const sum =(a:number ,b: number) => {
  return a+b;
}
sum(1,10);
class Persona {
  constructor(public age:number, public lastName:string){
  }
}

const deivit =new Persona(18,'torres');
deivit.age;
